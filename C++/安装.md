安装网址
```
https://www.msys2.org/
```
安装完成之后执行指令
```
pacman -Syyu -v //# -v 显示更多 verbose 信息
pacman -S --needed base-devel mingw-w64-x86_64-toolchain //更新软件包
```
将Gcc.exe路径添加到环境变量
cmd窗口下测试
```
gcc -v //查看gcc版本
```
vscode下安装对应插件
```
C/C++插件
code Runner 插件
```